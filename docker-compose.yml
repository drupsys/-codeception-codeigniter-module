version: '3'
services:
  codeception-module:
    build:
      context: .
      args:
        OS_UID: "${OS_UID}"
        OS_GID: "${OS_GID}"
    container_name: codeception-module
    entrypoint: tail -f /dev/null
    volumes:
    - .:/module
